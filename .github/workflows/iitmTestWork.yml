name: iitm tds, github action, test

on:
  schedule:
    # Runs at 12:00 UTC (noon) every day
    - cron: "0 12 * * *"
  workflow_dispatch: # Allows manual triggering

jobs:
  test:
    runs-on: ubuntu-latest  # required: specify the runner
    steps:
      - name: 23f2005575@ds.study.iitm.ac.in
        run: echo "Hello, world!"
